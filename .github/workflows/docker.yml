name: TestandoDocker

on:
  push:
    branches: [feature/docker]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Doing checkout
        uses: actions/checkout@v2

      - name: Print Hello World
        run: echo ${GITHUB_SHA::8}
      - name: Build image
        uses: mr-smithers-excellent/docker-build-push@v5
        with:
          image: felipeleonardo/flask-app
          registry: docker.io
          username: ${{ secrets.DOCKER_HUB_USER }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}
        run: docker build -t felipeleonardo/flask-app:${GITHUB_SHA::8} .
      # - name: Building
      #   uses: mr-smithers-excellent/docker-build-push@v5
      #   with:
      #     image: felipeleonardo/flask-app
      #     tags: ${GITHUB_SHA::8}
      #     registry: docker.io
      #     username: ${{ secrets.DOCKER_HUB_USER }}
      #     password: ${{ secrets.DOCKER_HUB_PASSWORD }}